import 'package:flutter/material.dart';
import '../../layouts/BaseLayout.dart';

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return BaseLayout(child: Container(child: Text("home")));
  }
}
